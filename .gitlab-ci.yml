image: node

stages:
  - test
  - build
  - deploy

compilação:
  stage: build

  artifacts:
    paths:
      - ./website/build/test-site

  script:
    - cd website
    - npm install
    - npm run build